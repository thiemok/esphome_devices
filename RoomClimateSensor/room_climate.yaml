<<: !include ../base.yaml

substitutions:
  room_name: "Bed Room"

esphome:
  name: room_climate
  platform: ESP8266
  board: d1_mini

i2c:
  sda: D2
  scl: D3

sensor:
  - platform: dht
    model: DHT11
    pin: D0
    temperature:
      name: "${room_name} Temperature"
    humidity:
      name: "${room_name} Humidity"
    update_interval: 120s
  - platform: tsl2561
    name: "${room_name} Ambient Light"
    address: 0x39
    update_interval: 120s
